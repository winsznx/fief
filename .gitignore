# dependencies
node_modules/
**/node_modules/
.pnp
.pnp.js

# builds / caches
.next/
out/
dist/
.turbo/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# 0G Compute seam spike run artifacts (generated locally; may embed live provider responses)
internal/seam/p0.2/report/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Qwen Code local settings (tool-local, not part of the product)
.qwen/

# Kilo agent-manager worktrees + session state (tool-local, not part of the product)
.kilo/

# NOTE: /internal/ is intentionally TRACKED for now so collaborators have full context.
# Per PRD §15 it is slated to be gitignored before public launch. Its history will
# persist in git, so decide on a history scrub (or keep the repo private) before then.

# Public deployment config for the Cloudflare build (addresses and tx hashes only).
!web/.env.production

# Campaign runtime artifacts. The record lives on chain; these are local traces.
*.out
*.spool.jsonl

# Supervisor lock directories.
running/*.lock
runtime/.campaign-*.lock/

# Video-prep scratch files, not project documentation.
internal/teleprompter.md
internal/teleprompter.docx
internal/video-runbook.md
